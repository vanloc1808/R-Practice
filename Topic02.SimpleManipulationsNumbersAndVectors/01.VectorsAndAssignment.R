x <- c(10.4, 5.6, 3.1, 6.4, 21.7); x #create a vector x from given numbers use c() function

#assignment can also be used by the function assign()
assign("x", c(10.4, 5.6, 3.1, 6.4, 21.7)); x

#the operator <- can also be used in other direction
c(10.4, 5.6, 3.1, 6.4, 21.7) -> x; x

1/x

y <- c(x, 0, x); y